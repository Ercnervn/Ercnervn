# .github/workflows/snake.yml
# Katkı grafiğini "yiyen" altın renkli yılan animasyonunu üretir ve `output` dalına gönderir.
# Generates the gold contribution-eating snake animation and pushes it to the `output` branch.

name: Generate Snake Animation

on:
  # Her gün 00:00 UTC'de otomatik çalışır / runs automatically every day at 00:00 UTC
  schedule:
    - cron: "0 0 * * *"
  # Actions sekmesinden elle tetiklenebilir / can be triggered manually from the Actions tab
  workflow_dispatch:
  # main dalına her push'ta çalışır / runs on every push to main
  push:
    branches:
      - main

jobs:
  generate:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Generate snake SVG (gold palette)
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-contribution-grid-snake.svg?color_snake=#E6B450&color_dots=#0D1117,#3A2F17,#7A5F22,#B78F34,#F2C879

      - name: Push to the output branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
